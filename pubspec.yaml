name: whisker_works
description: Mouse Colony Tracking
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  path_provider: ^2.1.5
  csv: ^6.0.0
  shared_preferences: ^2.5.3
  firebase_core: ^2.30.0
  cloud_firestore: ^4.17.5
  cupertino_icons: ^1.0.8
  rename: ^3.1.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.6
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
